{% extends 'base.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}


    <table class="table">
        <tbody>
            <tr>
                <th>Désignation</th>
                <td>{{ categorie.designation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <div class="container">
        <div class="row">
            <div class="col col-lg-3">
                <a href="{{ path('categorie_index') }}">
                    <span class="btn btn-primary"><i class="fa fa-backward"></i>&nbsp; Retour</span></a>&nbsp;
                <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">
                    <span class="btn btn-success"><i class="fa fa-backward"></i>&nbsp; Editer</span></a>
            </div>
            {{ include('categorie/_delete_form.html.twig') }}
        </div>
    </div>


{% endblock %}
